camera:
  index: 0
  width: 640
  height: 480
  fps: 30

model:
  path: "weights/best.onnx"
  confidence_threshold: 0.25
  cooldown_period: 5

zmq:
  frame_port: 5555
  detection_port: 5556
  control_port: 5557

server:
  host: "0.0.0.0"
  port: 5000
  debug: false

logging:
  level: "INFO"

serial:
  port: "COM3"
  baudrate: 115200
